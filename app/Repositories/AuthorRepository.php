<?php

namespace App\Repositories;

class AuthorRepository
{
    // insert or update meta data
    public function processMetaData($payload, $request)
    {
        $data = [];
        // $data['featured_image'] = $request->featured_image ?? null;
        // $data['facebook'] = $request->facebook ?? null;
        // $data['instagram'] = $request->instagram ?? null;
        // $data['youtube'] = $request->youtube ?? null;
        // $data['twitter'] = $request->twitter ?? null;
        // $data['tiktok'] = $request->tiktok ?? null;
        // $data['linkedin'] = $request->linkedin ?? null;

        return $data;
    }
}
